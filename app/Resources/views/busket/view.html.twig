{% extends 'base.html.twig' %}

{% block body %}
    <h2>Your Busket Is Here</h2>
    <table class="table table-hover">
        {% for item in items %}
            <tr>
                <td>{{ item.product }}</td>
                <td>{{ item.vat }}</td>
            </tr>
        {% endfor %}
    </table>
    <a class="btn btn-default" href="add" role="button">Add Items</a>
{% endblock %}
